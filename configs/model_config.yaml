# Agent 模型配置文件
# 方便统一管理和切换各个智能体使用的模型

agents:
  # 版面分析智能体 (Agent-1)
  modal_identification:
    model_name: "qwen3.5:9b"
    embedding_model: "nomic-embed-text"
    temperature: 0.0
    seed: 666
    dimensions: 768

  # 记忆反思智能体 (Memory Agent)
  memory_agent:
    model_name: "qwen3.5:27b"
    temperature: 0.0
    seed: 666
    enable_thinking: false

  # Step1 异构数据集代码生成智能体
  dataset_codegen_agent:
    model_name: "qwen3.5:9b"
    temperature: 0.0
    seed: 666
    mysql:
      host: "127.0.0.1"
      port: 3306
      user: "zyc_user"
      password: "1a2b3c=="
      database: "agent8"

  # 主协调者 (Main Orchestrator)
  main_orchestrator:
    model_name: "qwen3.5:9b"
    temperature: 0.0
    seed: 666

# 通用全局设置
global:
  default_temperature: 0.0
  default_seed: 666
